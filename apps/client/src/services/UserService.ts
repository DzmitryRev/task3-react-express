import { StatusType } from '../types/UserTypes';

export function removeUser(id: string) {
  // logic
}

export function ToggleBlockUser(id: string, status: StatusType) {
  // logic
}

export function ToggleBlockGroupUsers(ids: string[], status: StatusType) {
  // logic
}
